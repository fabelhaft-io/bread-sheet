# Partial backend config for the `dev` environment.
#   terraform init -backend-config=environments/dev.s3.tfbackend
# The bucket must exist before init — see infrastructure.md § Remote state bootstrap.
bucket = "breadsheet-tfstate"
key    = "dev/terraform.tfstate"
region = "us-east-1"
